using UnityEngine;

public class Crickets : MonoBehaviour
{
    // Вызывается один раз за каждый кадр
    void Update()
    {
        // Вывод слова crickets в консоль
        Debug.Log("crickets");
    }
}